<div class="full-screen">
  <div class="container-custom">
    <h2 class="text-center color-green">Connexion</h2>
    <hr class="thick-line mb-3">
    <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="form-inputs">
        <%= f.input :email,
                    required: true,
                    autofocus: true,
                    class: "input-field text-size-small background-blue",
                    input_html: { autocomplete: "email" } %>
        <%= f.input :password,
                    required: true,
                    label: "Mot de passe",
                    class: "input-field text-size-small background-blue",
                    input_html: { autocomplete: "current-password" } %>
        <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
      </div>

      <div class="form-actions">
        <%= f.button :submit, "Connexion", class: "button-small text-size-regular mb-3" %>
        <%- if devise_mapping.registerable? && controller_name != 'registrations' %>
          <%= link_to "Inscription", new_registration_path(resource_name), class: "mt-3 color-green text-size medium underline" %><br />
        <% end %>

        <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
          <%= link_to "Mot de passe oublié?", new_password_path(resource_name), class: "text-size-xsmall" %><br />
        <% end %>
      </div>
    <% end %>
  </div>
</div>
